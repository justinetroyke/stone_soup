<%= form_for [@recipe, @recipe_ingredient] do |f| %>

  <%= f.label :ingredient_amount %>
  <%= f.text_field :ingredient_amount %>

  <%= f.label :ingredient_id %>
  <%= f.collection_select :ingredient_id, Ingredient.all, :id, :item %>

  <%= f.submit 'Add' %>
<% end %>
